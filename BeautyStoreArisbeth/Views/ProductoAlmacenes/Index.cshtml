@model IEnumerable<BeautyStoreArisbeth.Models.ProductoAlmacen>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de productos en almacenes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idAlmacen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idAlmacen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProductoAlmacen }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProductoAlmacen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProductoAlmacen })
        </td>
    </tr>
}

</table>
