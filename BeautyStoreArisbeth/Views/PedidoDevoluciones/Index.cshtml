@model IEnumerable<BeautyStoreArisbeth.Models.PedidoDevolucion>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de pedidos de productos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idPedido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idDevolucion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idPedido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idDevolucion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPedidoDevolucion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPedidoDevolucion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPedidoDevolucion })
        </td>
    </tr>
}

</table>
