@model IEnumerable<BeautyStoreArisbeth.Models.CompraProducto>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de compras de productos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCompraProducto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCompraProducto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCompraProducto })
        </td>
    </tr>
}

</table>
